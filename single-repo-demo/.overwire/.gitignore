# Local-only Overwire files — keep these out of version control.
# secrets.yml IS tracked here (declarations only, values null) to match the
# multi-repo demos; never commit literal secret values into it.
secrets.enc.json
state/
cache/
