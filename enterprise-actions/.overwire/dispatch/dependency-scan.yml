# yaml-language-server: $schema=https://docs.overwire.io/schemas/dispatch-defaults.schema.json
event: workflow_call
inputs:
  node-version: "20"
  working-directory: .
  audit-level: high
  production-only: "true"
