name: CI then Deploy
runs:
  - id: ci
    workflow: ci.yml
    event: push
  - id: deploy
    workflow: deploy.yml
    event: workflow_dispatch
    payload: ../dispatch/deploy.yml
    continueOnError: true
