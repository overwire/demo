# Electron safeStorage encrypted secrets (machine-specific)
**/.overwire/secrets.enc.json

# Runtime state (generated by Overwire runs)
**/.overwire/state/
**/.overwire/cache/

# OS
.DS_Store

# Node (if npm ci is run inside the fixture repos)
node_modules/
