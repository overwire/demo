# yaml-language-server: $schema=https://docs.overwire.io/schemas/modes.schema.json
defaultMode: mock
jobs:
  audit: mock
steps:
  audit.0: mock
  audit.1: mock
  audit.2: mock
  audit.3: mock
  audit.4: mock
  audit.5: mock
