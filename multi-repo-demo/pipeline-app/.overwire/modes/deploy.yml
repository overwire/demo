# yaml-language-server: $schema=https://docs.overwire.io/schemas/modes.schema.json
defaultMode: mock
jobs:
  gate: mock
  deploy: mock
  notify: mock
steps:
  gate.0: mock
  deploy.0: mock
  deploy.1: mock
  notify.0: mock
  notify.1: mock
